// analyticsRoutes.js
const express = require('express');
const router = express.Router();

// Define your analytics list route
router.get('/list', (req, res) => {
  // Fetch and send analytics list data from MongoDB
});

module.exports = router;