// reportingRoutes.js
const express = require('express');
const router = express.Router();

// Define your reporting list route
router.get('/list', (req, res) => {
  // Fetch and send reporting list data from MongoDB
});

module.exports = router;